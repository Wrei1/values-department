rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TESTING RULES (remove before production)
    // Allows all reads/writes during testing to unblock submissions.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
